"use strict";

function multiply(a, b) {
  const operation = a * b;

  return operation;
}

const totalOperation = multiply(30, 3);
const finalOperation = multiply(2, 5);
const decesiveOperation = multiply(100, 100);

console.log(totalOperation);
console.log(finalOperation);
console.log(decesiveOperation);
