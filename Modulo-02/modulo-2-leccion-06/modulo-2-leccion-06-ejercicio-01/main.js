'use strict';

const adalaber1 = {
  name: 'Susana',
  age: 34,
  occupation: 'periodista',
};

console.log(
  `Mi nombre es ${adalaber1.name}, tengo ${adalaber1.age} años, y soy ${adalaber1.occupation}.`
);

const adalaber2 = {
  name: 'Rocío',
  age: 25,
  occupation: 'actriz',
};

console.log(
  `Mi nombre es ${adalaber2.name}, tengo ${adalaber2.age} años, y soy ${adalaber2.occupation}.`
);
